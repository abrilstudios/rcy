#!/usr/bin/env bash
# Runs audio-trim.py inside the project environment (needs the viz extra for plots).
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec uv run --project "$SCRIPT_DIR/../.." python "$SCRIPT_DIR/audio-trim.py" "$@"
